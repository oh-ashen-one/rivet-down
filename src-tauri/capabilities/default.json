{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Default desktop capability for the offline game shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
